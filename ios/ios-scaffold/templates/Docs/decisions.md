# Architecture decisions

- SwiftUI + MVVM
- Offline-first persistence boundary
- Push notifications plumbing included (stubbed until signing/APNs)
- Two environments: non-prod and prod via xcconfig
- Strict quality gates: formatting, linting, unit tests, UI test coverage for primary flows
